#include "ScreenshotHelper.h"

// Phase 1:空实现
// ScreenshotHelper 的 capture() 在头文件中为 inline 空体,
// 本 .cxx 仅用于触发 AUTOMOC 对 Q_OBJECT 的 moc 处理。
// Phase 3 会把 grabToImage + PNG 写盘 + manifest.jsonl 追加逻辑迁入此处。
