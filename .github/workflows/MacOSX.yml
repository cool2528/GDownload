name: MacOS-CI

on:
  push:
    tags: 
      - '*'
    branches:
      - 'main'
jobs:
  build:
    if: github.ref_type == 'tag'
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest]
        include:
          - qt_version: 6.5.2
            package_name: gdownloader_macos
            qt_arch: clang_64

    steps:
      - name: Checkout Branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 
          fetch-tags: true
          submodules: recursive

      - name: Setup Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ matrix.qt_version }}
          arch: ${{ matrix.qt_arch }}
          modules: 'qt3d qtimageformats qtwebsockets qt5compat qtshadertools'

      - name: Install Tools
        run: |
          brew install create-dmg
          brew install ninja

      - name: Setup vcpkg
        id: vcpkg
        uses: johnwason/vcpkg-action@v6
        with:
          manifest-dir: ${{ github.workspace }}
          extra-args: --overlay-triplets=${{ github.workspace }}/custom-triplets
          triplet: arm64-osx-release
          token: ${{ github.token }}
          github-binarycache: true

      - name: Build
        run: |
          echo "vcpkg-cmake-config: ${{ steps.vcpkg.outputs.vcpkg-cmake-config }}"
          cmake ${{ steps.vcpkg.outputs.vcpkg-cmake-config }} -B build -S ${{ github.workspace }} -DCMAKE_INSTALL_PREFIX=${{ github.workspace }}/macos_package -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_C_COMPILER=clang -GNinja
          cmake --build build --config Release --parallel
          cmake --install build --config Release
      - name: echo package name
        run: |
          echo "ls macos_package"
          ls macos_package

      - name: Upload Release
        if: startsWith(github.event.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.RELEASE_TOKEN }}
          file_glob: true
          file: macos_package/*
          overwrite: true
